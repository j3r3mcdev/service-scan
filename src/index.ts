/**
 * lib-scan — placeholder d’entrée.
 *
 * Ce fichier expose les éléments publics de la librairie.
 * Les détecteurs seront ajoutés ici au fur et à mesure.
 */

export const placeholder = {
  status: "ready",
  message: "lib-scan entrypoint placeholder",
};
